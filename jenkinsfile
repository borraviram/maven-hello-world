pipeline {
    agent any
    stages {
    stage('initialization'){
        steps {
            echo "Hello from initialization step"
        }
    }
    stage('build') {
        steps {
            echo "Hello from build step"
        }
    }
    }
}
